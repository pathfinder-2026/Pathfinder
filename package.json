{
  "name": "pathfinder",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pathfinder — AI teaching-and-learning platform for schools. MVP build per MVP Build Plan v1.4.",
  "workspaces": [
    "services/api",
    "infra",
    "apps/web",
    "apps/app"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "npm run test --workspace services/api && npm run test --workspace infra",
    "test:api": "npm run test --workspace services/api",
    "test:infra": "npm run test --workspace infra",
    "typecheck": "npm run typecheck --workspace services/api && npm run typecheck --workspace infra",
    "dev:api": "npm run dev --workspace services/api",
    "dev:web": "npm run dev --workspace apps/web",
    "dev:app": "npm run dev --workspace apps/app"
  }
}
